{
  "name": "top-email-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "worker-mailer": "^1.2.1"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
